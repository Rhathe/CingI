mission_plan_templates:
  one:
    mission_plan_templates:
      two:
        missions: echo onetwo
    missions:
      - echo one
      - extend_mission_plan: two
      - extend_mission_plan: three
  two:
    missions: echo two
  three:
    missions: echo three
  finished_plan:
    missions: echo nested_complete
missions:
  - extend_mission_plan: one
  - extend_mission_plan: two
  - mission_plan_templates:
      nested_plan:
        extend_mission_plan: finished_plan
    missions:
      - extend_mission_plan: nested_plan
  - echo "premature end"
  - extend_mission_plan: no_mission_plan
  - echo "end that should not be here"
