outpost:
  setup_steps:
    - echo "{}"; exit 0
missions:
  - echo "should run"
  - outpost:
     setup_steps:
      - echo "{}"; exit 7
    missions: echo "should not run"
  - echo "should also not run"
