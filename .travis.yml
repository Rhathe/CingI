language: elixir
elixir:
  - 1.5
install:
  - mix local.rebar --force # for Elixir 1.3.0 and up
  - mix local.hex --force
  - mix deps.get
  - mix escript.build
script:
  - ./cingi --file .cingi.yaml
